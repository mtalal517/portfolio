import { GoogleTagManager } from "@next/third-parties/google";
import { Inter } from "next/font/google";
import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";
import Footer from "./components/footer";
import ScrollToTop from "./components/helper/scroll-to-top";
import Navbar from "./components/navbar";
import "./css/card.scss";
import "./css/globals.scss";
const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "M Talal Qureshi",
  description:
    "This is the portfolio of M Talal Qureshi. I am a Full Stack Developer with a strong interest in machine learning, AI, and data-driven solutions. I have experience developing responsive websites and building machine learning models, backed by a solid foundation in mathematics, computer science, and Python programming. I'm also skilled in SQL, MongoDB, and big data tools like Hadoop, PySpark, and Apache Kafka. I'm always open to networking and collaboration. If you're into data science trends or have a project in mind, feel free to reach out.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ToastContainer />
        <main className="min-h-screen relative mx-auto px-6 sm:px-12 lg:max-w-[70rem] xl:max-w-[76rem] 2xl:max-w-[92rem] text-white">
          <Navbar />
          {children}
          <ScrollToTop />
        </main>
        <Footer />
      </body>
      <GoogleTagManager gtmId={process.env.NEXT_PUBLIC_GTM} />
    </html>
  );
}
